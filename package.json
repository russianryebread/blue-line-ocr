{
  "name": "hockey-scorecard-ocr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute hockey-scorecards --local --file=./migrations/0001_init.sql",
    "db:init:remote": "wrangler d1 execute hockey-scorecards --remote --file=./migrations/0001_init.sql",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.23.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^5.20250101.0"
  },
  "dependencies": {
    "hono": "^4.6.0"
  }
}
